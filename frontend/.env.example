# Set to 'true' to enable the 3-step onboarding wizard + placement quiz for new users.
# When unset or anything other than 'true', a user is created automatically on first visit.
VITE_ONBOARDING_ENABLED=true
